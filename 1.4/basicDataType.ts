{
  //Basic data type of typescript

  //primitive data type

  // string
  const myName: string = "Mr X";

  //number
  const age: number = 99;

  //boolean
  const isEmployed: boolean = false;

  //undefined
  const x: undefined = undefined;

  // null
    const y: null = null;
    
    //non-primitive data type

    //array
    let friends: string[] = ['a', 'b', 'c'];
    let friendsAge: number[] = [11, 12, 13];

    //tuple / ordered-array / controled-index-array
    let coordinates: [number, number] = [63, 25];
    let person: [string, number,boolean] = ["Mr x", 69, true];



}
